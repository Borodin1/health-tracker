import { Lunch } from '../../bus/questions/components/Lunch';
import { Base } from '../../views/base';

export const LunchPage = () => {
    return (
        <>
            <Base>
                <Lunch />
            </Base>
        </>
    );
};
