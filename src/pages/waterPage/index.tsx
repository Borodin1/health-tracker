import { Water } from '../../bus/questions/components/Water';
import { Base } from '../../views/base';

export const WaterPage = () => {
    return (
        <>
            <Base>
                <Water />
            </Base>
        </>
    );
};
