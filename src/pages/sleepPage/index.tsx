import { Sleep } from '../../bus/questions/components/Sleep';
import { Base } from '../../views/base';

export const SleepPage = () => {
    return (
        <>
            <Base>
                <Sleep />
            </Base>
        </>
    );
};
