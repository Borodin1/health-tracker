import { Junk } from '../../bus/questions/components/Junk';
import { Base } from '../../views/base';

export const JunkPage = () => {
    return (
        <>
            <Base>
                <Junk />
            </Base>
        </>
    );
};
